# Phase 2: 책 관리 기능 테스트

**단계:** 2/9  
**예상 소요 시간:** 15-20분  
**필수 선행 조건:** Phase 1 완료 (로그인 및 온보딩 완료)

---

## 🎯 테스트 목표

1. 책 검색 기능이 정상 작동하는지 확인
2. 책 추가 기능이 정상 작동하는지 확인
3. 책 목록 조회가 정상 작동하는지 확인
4. 책 상세 조회가 정상 작동하는지 확인
5. 책 상태 변경이 정상 작동하는지 확인

---

## 📋 테스트 시나리오

### 시나리오 2-1: 책 검색

**프롬프트:**
```
로그인 상태에서:

1. 헤더의 검색 바 또는 "내 서재" 메뉴를 통해 책 검색 페이지(/books/search)로 이동합니다
2. 검색어를 입력합니다 (예: "해리포터", "1984", "데미안")
3. 검색 결과가 표시되는지 확인합니다
4. 검색 결과에서 책 표지, 제목, 저자, 출판사 정보가 정상적으로 표시되는지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다
6. 네트워크 탭에서 네이버 책 검색 API 요청이 성공적으로 완료되었는지 확인합니다

확인 사항:
- 검색어 입력이 정상적으로 작동하는가?
- 검색 결과가 정상적으로 표시되는가?
- 책 정보(표지, 제목, 저자)가 정확하게 표시되는가?
- 검색 결과가 없을 때 적절한 메시지가 표시되는가?
```

**예상 결과:**
- 검색 결과 목록 표시
- 책 표지 이미지 로드
- 책 정보 정확히 표시

---

### 시나리오 2-2: 책 추가

**프롬프트:**
```
책 검색 결과 페이지에서:

1. 검색 결과 중 하나의 책을 선택합니다
2. "서재에 추가" 또는 "추가" 버튼을 클릭합니다
3. 책이 서재에 추가되었는지 확인합니다 (토스트 메시지 또는 리다이렉트)
4. 개발자 도구 콘솔에서 에러가 없는지 확인합니다
5. 네트워크 탭에서 책 추가 API 요청이 성공적으로 완료되었는지 확인합니다
6. 같은 책을 다시 추가하려고 시도합니다
7. 중복 추가 방지 메시지가 표시되는지 확인합니다

확인 사항:
- 책 추가가 성공적으로 완료되는가?
- 중복 추가가 방지되는가?
- 추가 후 적절한 피드백이 제공되는가?
```

**예상 결과:**
- 책 추가 성공 토스트 메시지
- 서재 목록에 추가된 책 표시
- 중복 추가 시 에러 메시지 표시

---

### 시나리오 2-3: 책 목록 조회

**프롬프트:**
```
내 서재 페이지(/books)로 이동합니다:

1. 페이지가 정상적으로 로드되는지 확인합니다
2. 추가한 책들이 목록에 표시되는지 확인합니다
3. 책 표지, 제목, 저자, 상태가 정상적으로 표시되는지 확인합니다
4. 책 상태 배지(읽기 전, 읽는 중, 읽음)가 정상적으로 표시되는지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다
6. 네트워크 탭에서 책 목록 조회 API 요청이 성공적으로 완료되었는지 확인합니다

확인 사항:
- 책 목록이 정상적으로 표시되는가?
- 책 정보가 정확하게 표시되는가?
- 책 상태가 정확하게 표시되는가?
- 책이 없을 때 적절한 메시지가 표시되는가?
```

**예상 결과:**
- 책 목록 정상 표시
- 책 정보 정확히 표시
- 책 상태 배지 정상 표시

---

### 시나리오 2-4: 책 상세 조회

**프롬프트:**
```
내 서재 페이지에서:

1. 책 목록 중 하나를 클릭합니다
2. 책 상세 페이지(/books/[id])로 이동하는지 확인합니다
3. 책 상세 정보(표지, 제목, 저자, 출판사, ISBN 등)가 정상적으로 표시되는지 확인합니다
4. 책 상태가 정상적으로 표시되는지 확인합니다
5. 시작일, 완독일 정보가 정상적으로 표시되는지 확인합니다
6. 해당 책의 기록 목록이 표시되는지 확인합니다
7. 개발자 도구 콘솔에서 에러가 없는지 확인합니다
8. 네트워크 탭에서 책 상세 조회 API 요청이 성공적으로 완료되었는지 확인합니다

확인 사항:
- 책 상세 정보가 정확하게 표시되는가?
- 책 상태가 정확하게 표시되는가?
- 기록 목록이 정상적으로 표시되는가?
- 기록이 없을 때 적절한 메시지가 표시되는가?
```

**예상 결과:**
- 책 상세 정보 정상 표시
- 기록 목록 정상 표시
- 기록이 없을 때 "기록 작성하기" 버튼 표시

---

### 시나리오 2-5: 책 상태 변경

**프롬프트:**
```
책 상세 페이지에서:

1. 책 상태 선택 드롭다운을 찾습니다
2. 현재 상태를 확인합니다
3. 다른 상태로 변경합니다 (예: "읽기 전" → "읽는 중")
4. 상태 변경이 성공적으로 저장되는지 확인합니다 (토스트 메시지 또는 UI 업데이트)
5. 페이지를 새로고침합니다
6. 변경된 상태가 유지되는지 확인합니다
7. 개발자 도구 콘솔에서 에러가 없는지 확인합니다
8. 네트워크 탭에서 상태 변경 API 요청이 성공적으로 완료되었는지 확인합니다

확인 사항:
- 상태 변경 드롭다운이 정상적으로 작동하는가?
- 상태 변경이 성공적으로 저장되는가?
- 상태 변경 후 UI가 즉시 업데이트되는가?
- 새로고침 후에도 변경된 상태가 유지되는가?
```

**예상 결과:**
- 상태 변경 성공 토스트 메시지
- 상태 배지 즉시 업데이트
- 새로고침 후에도 변경된 상태 유지

---

### 시나리오 2-6: 책 상태별 필터링 (선택사항)

**프롬프트:**
```
내 서재 페이지에서:

1. 상태별 필터가 있는지 확인합니다
2. 각 상태별로 필터링해봅니다 (읽기 전, 읽는 중, 읽음)
3. 필터링된 결과가 정확한지 확인합니다
4. 필터 해제 시 모든 책이 표시되는지 확인합니다

확인 사항:
- 상태별 필터가 정상적으로 작동하는가?
- 필터링된 결과가 정확한가?
```

**예상 결과:**
- 상태별 필터 정상 작동
- 필터링된 결과 정확히 표시

---

## ✅ 체크리스트

각 시나리오를 완료한 후 다음을 확인하세요:

- [ ] 책 검색이 정상 작동함
- [ ] 책 추가가 정상 작동함
- [ ] 중복 추가 방지가 정상 작동함
- [ ] 책 목록 조회가 정상 작동함
- [ ] 책 상세 조회가 정상 작동함
- [ ] 책 상태 변경이 정상 작동함
- [ ] 상태 변경 후 데이터가 정확하게 저장됨
- [ ] 에러 없이 모든 기능이 작동함

---

## 🐛 발견 사항 기록

### 정상 작동 기능
- 

### 발견된 버그
- 

### 개선 제안
- 

### 성능 이슈
- 

---

## 📸 스크린샷 (필요시)

중요한 화면이나 에러 발생 시 스크린샷을 첨부하세요.

---

## ➡️ 다음 단계

Phase 2가 완료되면 **Phase 3: 기록 관리 기능 테스트 (기본)**로 진행하세요.

---

**문서 끝**

