# Phase 7: 통계 및 타임라인 테스트

**단계:** 7/9  
**예상 소요 시간:** 10-15분  
**필수 선행 조건:** Phase 2, 3, 4 완료 (책 및 기록 작성 완료)

---

## 🎯 테스트 목표

1. 대시보드 통계가 정상 작동하는지 확인
2. 목표 진행률이 정상 작동하는지 확인
3. 타임라인이 정상 작동하는지 확인
4. 타임라인 정렬이 정상 작동하는지 확인

---

## 📋 테스트 시나리오

### 시나리오 7-1: 대시보드 통계 조회

**프롬프트:**
```
메인 페이지(/)로 이동합니다:

1. 페이지가 정상적으로 로드되는지 확인합니다
2. 대시보드 통계 영역을 확인합니다
3. 다음 통계가 정상적으로 표시되는지 확인합니다:
   - 읽은 책 수
   - 작성한 기록 수
   - 목표 진행률
   - 월별 통계
4. 통계 수치가 정확한지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다
6. 네트워크 탭에서 통계 API 요청이 성공적으로 완료되었는지 확인합니다

확인 사항:
- 대시보드 통계가 정상적으로 표시되는가?
- 통계 수치가 정확한가?
- 통계가 실시간으로 업데이트되는가?
```

**예상 결과:**
- 대시보드 통계 정상 표시
- 통계 수치 정확
- 통계 실시간 업데이트

---

### 시나리오 7-2: 목표 진행률 확인

**프롬프트:**
```
대시보드에서:

1. 목표 진행률 영역을 확인합니다
2. 목표 책 수와 현재 읽은 책 수가 정상적으로 표시되는지 확인합니다
3. 진행률 바(프로그레스 바)가 정상적으로 표시되는지 확인합니다
4. 진행률 퍼센트가 정확한지 확인합니다
5. 목표 달성 시 적절한 메시지가 표시되는지 확인합니다
6. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 목표 진행률이 정상적으로 표시되는가?
- 진행률 수치가 정확한가?
- 진행률 바가 정확하게 표시되는가?
- 목표 달성 시 적절한 피드백이 제공되는가?
```

**예상 결과:**
- 목표 진행률 정상 표시
- 진행률 수치 정확
- 진행률 바 정확히 표시
- 목표 달성 시 축하 메시지 표시

---

### 시나리오 7-3: 월별 통계 확인

**프롬프트:**
```
대시보드에서:

1. 월별 통계 영역을 확인합니다
2. 각 월별 읽은 책 수와 작성한 기록 수가 정상적으로 표시되는지 확인합니다
3. 차트 또는 그래프가 정상적으로 표시되는지 확인합니다 (있는 경우)
4. 월별 통계 수치가 정확한지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 월별 통계가 정상적으로 표시되는가?
- 월별 통계 수치가 정확한가?
- 차트/그래프가 정상적으로 표시되는가?
```

**예상 결과:**
- 월별 통계 정상 표시
- 월별 통계 수치 정확
- 차트/그래프 정상 표시

---

### 시나리오 7-4: 타임라인 조회

**프롬프트:**
```
타임라인 페이지(/timeline)로 이동합니다:

1. 페이지가 정상적으로 로드되는지 확인합니다
2. 타임라인에 기록들이 정상적으로 표시되는지 확인합니다
3. 기록들이 날짜순으로 정렬되어 있는지 확인합니다
4. 각 기록의 정보(유형, 내용, 책 제목, 날짜)가 정상적으로 표시되는지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다
6. 네트워크 탭에서 타임라인 API 요청이 성공적으로 완료되었는지 확인합니다

확인 사항:
- 타임라인이 정상적으로 표시되는가?
- 기록들이 정확하게 표시되는가?
- 기록 정보가 정확하게 표시되는가?
- 기록이 없을 때 적절한 메시지가 표시되는가?
```

**예상 결과:**
- 타임라인 정상 표시
- 기록 정확히 표시
- 기록 정보 정확히 표시
- 기록 없을 때 "기록이 없습니다" 메시지

---

### 시나리오 7-5: 타임라인 정렬 (최신순)

**프롬프트:**
```
타임라인 페이지에서:

1. 정렬 옵션을 찾습니다
2. "최신순"을 선택합니다
3. 기록들이 최신순으로 정렬되는지 확인합니다
4. 가장 최근 기록이 맨 위에 표시되는지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 최신순 정렬이 정상적으로 작동하는가?
- 기록들이 정확하게 정렬되는가?
```

**예상 결과:**
- 최신순 정렬 정상 작동
- 기록 정확히 정렬

---

### 시나리오 7-6: 타임라인 정렬 (오래된순)

**프롬프트:**
```
타임라인 페이지에서:

1. 정렬 옵션을 선택합니다
2. "오래된순"을 선택합니다
3. 기록들이 오래된순으로 정렬되는지 확인합니다
4. 가장 오래된 기록이 맨 위에 표시되는지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 오래된순 정렬이 정상적으로 작동하는가?
- 기록들이 정확하게 정렬되는가?
```

**예상 결과:**
- 오래된순 정렬 정상 작동
- 기록 정확히 정렬

---

### 시나리오 7-7: 타임라인 정렬 (책별)

**프롬프트:**
```
타임라인 페이지에서:

1. 정렬 옵션을 선택합니다
2. "책별"을 선택합니다
3. 기록들이 책별로 그룹화되어 표시되는지 확인합니다
4. 같은 책의 기록들이 함께 표시되는지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 책별 정렬이 정상적으로 작동하는가?
- 기록들이 책별로 정확하게 그룹화되는가?
```

**예상 결과:**
- 책별 정렬 정상 작동
- 기록 책별로 정확히 그룹화

---

### 시나리오 7-8: 타임라인 페이지네이션

**프롬프트:**
```
타임라인에 기록이 많은 경우:

1. 타임라인 페이지에서 페이지네이션을 확인합니다
2. 페이지 번호 또는 "다음" 버튼을 클릭합니다
3. 다음 페이지의 기록이 정상적으로 표시되는지 확인합니다
4. "이전" 버튼을 클릭합니다
5. 이전 페이지의 기록이 정상적으로 표시되는지 확인합니다
6. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 페이지네이션이 정상적으로 작동하는가?
- 페이지 이동이 정상적으로 작동하는가?
- 기록이 정확하게 표시되는가?
```

**예상 결과:**
- 페이지네이션 정상 작동
- 페이지 이동 정상 작동
- 기록 정확히 표시

---

### 시나리오 7-9: 타임라인 월별 그룹화

**프롬프트:**
```
타임라인 페이지에서:

1. 기록들이 월별로 그룹화되어 표시되는지 확인합니다
2. 각 월의 헤더가 정상적으로 표시되는지 확인합니다 (예: "2024년 12월")
3. 해당 월의 기록들이 그룹 아래에 표시되는지 확인합니다
4. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 월별 그룹화가 정상적으로 작동하는가?
- 월 헤더가 정확하게 표시되는가?
- 기록들이 정확하게 그룹화되는가?
```

**예상 결과:**
- 월별 그룹화 정상 작동
- 월 헤더 정확히 표시
- 기록 정확히 그룹화

---

## ✅ 체크리스트

각 시나리오를 완료한 후 다음을 확인하세요:

- [ ] 대시보드 통계가 정상 작동함
- [ ] 목표 진행률이 정상 작동함
- [ ] 월별 통계가 정상 작동함
- [ ] 타임라인 조회가 정상 작동함
- [ ] 타임라인 정렬(최신순)이 정상 작동함
- [ ] 타임라인 정렬(오래된순)이 정상 작동함
- [ ] 타임라인 정렬(책별)이 정상 작동함
- [ ] 타임라인 페이지네이션이 정상 작동함
- [ ] 타임라인 월별 그룹화가 정상 작동함
- [ ] 에러 없이 모든 기능이 작동함

---

## 🐛 발견 사항 기록

### 정상 작동 기능
- 

### 발견된 버그
- 

### 개선 제안
- 

### 성능 이슈
- 

---

## 📸 스크린샷 (필요시)

중요한 화면이나 에러 발생 시 스크린샷을 첨부하세요.

---

## ➡️ 다음 단계

Phase 7이 완료되면 **Phase 8: 독서모임 기능 테스트**로 진행하세요.

---

**문서 끝**

