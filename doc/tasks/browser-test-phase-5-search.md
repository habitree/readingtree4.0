# Phase 5: 검색 기능 테스트

**단계:** 5/9  
**예상 소요 시간:** 10-15분  
**필수 선행 조건:** Phase 3, 4 완료 (다양한 기록 작성 완료)

---

## 🎯 테스트 목표

1. 기록 검색 기능이 정상 작동하는지 확인
2. 검색 필터가 정상 작동하는지 확인
3. 검색어 하이라이트가 정상 작동하는지 확인
4. 페이지네이션이 정상 작동하는지 확인

---

## 📋 테스트 시나리오

### 시나리오 5-1: 기본 검색

**프롬프트:**
```
검색 페이지(/search)로 이동합니다:

1. 헤더의 검색 바 또는 검색 페이지로 이동합니다
2. 검색어를 입력합니다 (예: 기록에 포함된 텍스트)
3. 검색 결과가 표시되는지 확인합니다
4. 검색 결과에서 기록 정보(유형, 내용, 책 제목, 날짜)가 정상적으로 표시되는지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다
6. 네트워크 탭에서 검색 API 요청이 성공적으로 완료되었는지 확인합니다

확인 사항:
- 검색어 입력이 정상적으로 작동하는가?
- 검색 결과가 정상적으로 표시되는가?
- 검색 결과가 정확한가?
- 검색 결과가 없을 때 적절한 메시지가 표시되는가?
```

**예상 결과:**
- 검색 결과 목록 표시
- 검색 결과 정확
- 검색 결과 없을 때 "검색 결과가 없습니다" 메시지

---

### 시나리오 5-2: 한글 검색

**프롬프트:**
```
검색 페이지에서:

1. 한글 검색어를 입력합니다 (예: "인상", "명언", "깊은")
2. 검색 결과가 정상적으로 표시되는지 확인합니다
3. 한글 검색어가 정확하게 매칭되는지 확인합니다
4. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 한글 검색이 정상적으로 작동하는가?
- 한글 검색 결과가 정확한가?
```

**예상 결과:**
- 한글 검색 정상 작동
- 한글 검색 결과 정확

---

### 시나리오 5-3: 검색 필터 (책별)

**프롬프트:**
```
검색 페이지에서:

1. 검색 필터 영역을 찾습니다
2. "책" 필터를 선택합니다
3. 특정 책을 선택합니다
4. 선택한 책의 기록만 검색 결과에 표시되는지 확인합니다
5. 필터를 해제합니다
6. 모든 기록이 검색 결과에 표시되는지 확인합니다
7. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 책 필터가 정상적으로 작동하는가?
- 필터링된 결과가 정확한가?
- 필터 해제가 정상적으로 작동하는가?
```

**예상 결과:**
- 책 필터 정상 작동
- 필터링된 결과 정확
- 필터 해제 정상 작동

---

### 시나리오 5-4: 검색 필터 (날짜별)

**프롬프트:**
```
검색 페이지에서:

1. "날짜" 필터를 선택합니다
2. 시작일과 종료일을 선택합니다
3. 선택한 날짜 범위의 기록만 검색 결과에 표시되는지 확인합니다
4. 필터를 해제합니다
5. 모든 기록이 검색 결과에 표시되는지 확인합니다
6. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 날짜 필터가 정상적으로 작동하는가?
- 필터링된 결과가 정확한가?
- 날짜 범위 선택이 정상적으로 작동하는가?
```

**예상 결과:**
- 날짜 필터 정상 작동
- 필터링된 결과 정확
- 날짜 범위 선택 정상 작동

---

### 시나리오 5-5: 검색 필터 (태그별)

**프롬프트:**
```
검색 페이지에서:

1. "태그" 필터를 선택합니다
2. 특정 태그를 선택합니다
3. 선택한 태그가 포함된 기록만 검색 결과에 표시되는지 확인합니다
4. 필터를 해제합니다
5. 모든 기록이 검색 결과에 표시되는지 확인합니다
6. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 태그 필터가 정상적으로 작동하는가?
- 필터링된 결과가 정확한가?
- 태그 선택이 정상적으로 작동하는가?
```

**예상 결과:**
- 태그 필터 정상 작동
- 필터링된 결과 정확
- 태그 선택 정상 작동

---

### 시나리오 5-6: 검색 필터 (유형별)

**프롬프트:**
```
검색 페이지에서:

1. "유형" 필터를 선택합니다
2. 특정 유형을 선택합니다 (예: "필사", "메모", "사진")
3. 선택한 유형의 기록만 검색 결과에 표시되는지 확인합니다
4. 필터를 해제합니다
5. 모든 기록이 검색 결과에 표시되는지 확인합니다
6. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 유형 필터가 정상적으로 작동하는가?
- 필터링된 결과가 정확한가?
- 유형 선택이 정상적으로 작동하는가?
```

**예상 결과:**
- 유형 필터 정상 작동
- 필터링된 결과 정확
- 유형 선택 정상 작동

---

### 시나리오 5-7: 검색 필터 조합

**프롬프트:**
```
검색 페이지에서:

1. 여러 필터를 동시에 선택합니다 (예: 책 + 날짜 + 태그)
2. 모든 필터 조건을 만족하는 기록만 검색 결과에 표시되는지 확인합니다
3. 필터를 하나씩 해제해봅니다
4. 검색 결과가 동적으로 업데이트되는지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 여러 필터 조합이 정상적으로 작동하는가?
- 필터링된 결과가 정확한가?
- 필터 해제 시 검색 결과가 동적으로 업데이트되는가?
```

**예상 결과:**
- 여러 필터 조합 정상 작동
- 필터링된 결과 정확
- 필터 해제 시 동적 업데이트

---

### 시나리오 5-8: 검색어 하이라이트

**프롬프트:**
```
검색 결과에서:

1. 검색 결과 목록을 확인합니다
2. 검색어가 기록 내용에서 하이라이트되어 표시되는지 확인합니다
3. 여러 검색어를 입력해봅니다
4. 모든 검색어가 하이라이트되는지 확인합니다
5. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 검색어 하이라이트가 정상적으로 작동하는가?
- 하이라이트가 정확한가?
- 여러 검색어가 모두 하이라이트되는가?
```

**예상 결과:**
- 검색어 하이라이트 정상 작동
- 하이라이트 정확
- 여러 검색어 모두 하이라이트

---

### 시나리오 5-9: 페이지네이션

**프롬프트:**
```
검색 결과가 많은 경우:

1. 검색 결과가 여러 페이지로 나뉘어 있는지 확인합니다
2. 페이지 번호 또는 "다음" 버튼을 클릭합니다
3. 다음 페이지의 검색 결과가 정상적으로 표시되는지 확인합니다
4. "이전" 버튼을 클릭합니다
5. 이전 페이지의 검색 결과가 정상적으로 표시되는지 확인합니다
6. 개발자 도구 콘솔에서 에러가 없는지 확인합니다

확인 사항:
- 페이지네이션이 정상적으로 작동하는가?
- 페이지 이동이 정상적으로 작동하는가?
- 검색 결과가 정확하게 표시되는가?
```

**예상 결과:**
- 페이지네이션 정상 작동
- 페이지 이동 정상 작동
- 검색 결과 정확히 표시

---

## ✅ 체크리스트

각 시나리오를 완료한 후 다음을 확인하세요:

- [ ] 기본 검색이 정상 작동함
- [ ] 한글 검색이 정상 작동함
- [ ] 책 필터가 정상 작동함
- [ ] 날짜 필터가 정상 작동함
- [ ] 태그 필터가 정상 작동함
- [ ] 유형 필터가 정상 작동함
- [ ] 필터 조합이 정상 작동함
- [ ] 검색어 하이라이트가 정상 작동함
- [ ] 페이지네이션이 정상 작동함
- [ ] 에러 없이 모든 기능이 작동함

---

## 🐛 발견 사항 기록

### 정상 작동 기능
- 

### 발견된 버그
- 

### 개선 제안
- 

### 성능 이슈
- 

---

## 📸 스크린샷 (필요시)

중요한 화면이나 에러 발생 시 스크린샷을 첨부하세요.

---

## ➡️ 다음 단계

Phase 5가 완료되면 **Phase 6: 공유 기능 테스트**로 진행하세요.

---

**문서 끝**

